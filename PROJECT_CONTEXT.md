# 项目背景

## 项目名称
经营沙盘 Demo

## 项目目标
这是一个用于向领导汇报和争取资源的经营沙盘 Demo。

当前目标不是做完整系统，而是做一个“现在就能演示、能让领导看出价值、愿意继续投资源”的理想版本。

## 核心表达
整个沙盘围绕三个模块展开：

- 业务经营：看结果
- 运营动作：看动作
- 技术支撑：看支撑

三者共同构成“结果、动作、支撑”的价值评估闭环。

## 统一分析框架
为了让不同模块的分析方式一致，模块内部统一采用三级指标结构：

- 一级指标：看模块整体结果
- 二级指标：看模块结构强弱
- 三级指标：看过程明细和原因解释

设计原则：

- 一级指标相对稳定，用于跨周期比较
- 二级指标按业务阶段适配
- 三级指标用于解释结果和定位原因

## 业务阶段
当前 Demo 支持按业务阶段切换展示：

- 增长验证期
- 成熟 / 现金牛
- 增长放量期
- 创新 / 探索

不同阶段会影响三大模块的关注重点。

## 当前 Demo 定位
当前版本是“理想演示版”，主要用于讲清楚：

1. 这套经营沙盘为什么有价值
2. 为什么现在值得开始做
3. 为什么后续值得继续投资源

因此当前版本不强调“所有数据已经真实打通”，而强调：

- 经营框架已经清晰
- 模块逻辑已经完整
- 管理层价值已经可以被看见
- 未来 AI 化方向已经可以被说明

## 页面结构
当前页面主要由以下部分组成：

1. 首页总览
展示经营沙盘定位、当前阶段焦点、管理层核心问题

2. 统一分析方法
说明一级、二级、三级指标的统一结构

3. 业务阶段切换
支持按不同业务阶段切换展示逻辑

4. 三大模块切换
- 业务经营
- 运营动作
- 技术支撑

每个模块下都展示：
- 一级指标
- 二级指标
- 三级指标
- 当前汇报建议

5. 当前版本定位
说明为什么当前先做理想演示版

6. 未来 AI 化
展示未来如何从价值评估升级到经营分析与决策辅助

## AI 化方向
未来 AI 化不是简单增加一个问答入口，而是推动系统从“价值评估”升级为“经营分析与决策辅助”。

当前采用的表达路径是：

- Data Flow：先把项目、阶段、动作、支撑、投入、产出连起来
- AI Flow：再让系统做解释、预判、预警和资源建议
- Workflow：最后把建议送进复盘、跟进和协同闭环

同时借鉴了 AIP / Ontology 的方法论，但不直接照搬重平台形态。

## 当前代码说明
当前 Demo 是基于 React + Vite + TypeScript 前端项目改造而来。

当前主要文件：

- src/App.tsx：页面内容与假数据结构
- src/App.css：页面样式
- src/index.css：全局视觉基底

## 当前状态
当前页面已经可以正常 build，并可通过本地 preview 进行预览。

## 后续建议方向
下一步优先级建议如下：

1. 增加更像管理驾驶舱的首页总览
2. 补一个更真实的业务案例穿透页
3. 增加图表、筛选、时间维度等更强的数据表达
4. 让 AI 部分更像经营决策助手，而不是静态说明
5. 逐步替换部分假数据为真实业务数据或半真实案例

## 换电脑迁移建议
迁移时至少带走以下内容：

- 整个 AIP 文件夹
- 本文件 PROJECT_CONTEXT.md
- HANDOFF_PROMPT.md

新电脑打开后执行：

```bash
npm install
npm run build
npm run dev
```

如果 dev 环境不稳定，也可以先用：

```bash
npm run preview
```
